<p style="color: green"><%= notice %></p>

<%= render @mining_type %>

<div>
  <%= link_to "#{t('edit')}#{t('mining_type')}", edit_mining_type_path(@mining_type) %> |
  <%= link_to "#{t('back')}", mining_types_path %>

  <%= button_to t('remove'), @mining_type, method: :delete %>
  
</div>
